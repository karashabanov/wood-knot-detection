dataset:
  images: data/images
  labels: data/labels

splits:
  directory: data/splits
  train_ratio: 0.70
  validation_ratio: 0.15
  test_ratio: 0.15
  seed: 42

output:
  directory: output

models:
  weights: models